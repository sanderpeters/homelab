apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  parentRefs:
    - name: traefik
      namespace: traefik
  hostnames:
    - argocd.sanderpeters.cloud
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
