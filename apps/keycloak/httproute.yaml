apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: keycloak
  namespace: keycloak
spec:
  parentRefs:
    - name: traefik
      namespace: traefik
  hostnames:
    - login.sanderpeters.cloud
  rules:
    - backendRefs:
        - name: keycloak-service
          port: 8080
