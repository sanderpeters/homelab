apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: journiv
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/sanderpeters/homelab
    targetRevision: master
    path: apps/journiv
  destination:
    server: https://kubernetes.default.svc
    namespace: journiv
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
